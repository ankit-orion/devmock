import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev-tools indicator (the logo button in the corner during development)
  devIndicators: false,
};

export default nextConfig;
